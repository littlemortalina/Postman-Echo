image: Ubuntu2004
stack: jdk 11

install:
  - sudo apt-get update -y
  - sudo apt-get install -y ca-certificates
  - sudo update-ca-certificates --fresh
  - chmod +x gradlew

test_script:
  - ./gradlew test --info